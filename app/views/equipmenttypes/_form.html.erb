<%= form_for(@equipmenttype) do |f| %>
  <% if @equipmenttype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipmenttype.errors.count, "error") %> prohibited this equipmenttype from being saved:</h2>

      <ul>
      <% @equipmenttype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
